### RPP add-on

Read **`docs/addons/rpp/README.md`** and wisdom topic **pattern-authority** (`~/develop/dighum_template/wisdom/topics/pattern-authority.md` or `docs/wisdom/` if bootstrapped with `--with-wisdom`).

- Metric name is **RPP** only (not PRP) in new code and manifests.
- **Pattern authority:** one canonical source per era — flag unmatched rows; never invent patterns.
- **Presidents:** separate track from delegate RPP; do not mix into raw-pattern joins.
- Never use `datetime` before 1678 — use `pd.Period`.

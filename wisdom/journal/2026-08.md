# 2026-08

## 2026-08-22 — Cost-sensitive workflow (wvo_corr pilot)

- **`wvo_corr`** Bayesian + Observable publication plan split into:
  - `PLAN.md` (checklist)
  - `docs/steps/STEP*.md` (per-step guides; newer convention: `plans/steps/`)
  - `docs/steps/MODEL-TIERS.md` (project handoff table)
  - `AGENTS.md` + `.cursor/rules/`
- Key savings: **user runs terminal**; Ask mode for commands; simple Agent for single-file boilerplate; strong model for PyMC specs only.
- Promoted to wisdom: [cost-sensitive-agent-workflow](../topics/cost-sensitive-agent-workflow.md).

## 2026-09-14 — plans/ layout

- Root `PLAN.md` = headlines only in bootstrapped projects (thin template; no `plans/` stubs copied).
- Multi-step work creates `plans/` on demand: full plans, `milestones/`, `steps/`.
- Maintainer plans in dighum_template live under `plans/` (not `docs/`).
- workflow MCP prefers `plans/steps/`, falls back to `docs/steps/`.

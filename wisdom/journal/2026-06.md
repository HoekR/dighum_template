# 2026-06

## 2026-06-30 — Template repo + layers

- Split bootstrap into dedicated **`dighum_template`** repo (away from `republic_ner_matching`).
- Added **workspace profile** (`.vscode/`, `.code-workspace`, `.cursor/rules/`) to bootstrap output.
- Introduced **`wisdom/`** for durable cross-project lessons vs per-project `docs/`.
- Introduced **`addons/`** for optional domain overlays (RPP, TRIFECTA) applied at bootstrap or later.
- `data_io` still developed in `republic_ner_matching`; vendored in `dighum_template/packages/data_io/`.

## 2026-06-29 — Pattern join coverage (gnb_analysis)

- ~69% raw pattern match on spatial key `offset|d|m|j`; ~130k `final_release` rows unmatched.
- Treat gap as correction-pipeline additions — flag, don't backfill from streamlit.
